= form_with(model: step, local: true) do |form|
  - if step.errors.any?
    #error_explanation
      %h2
        = pluralize(step.errors.count, "error")
        prohibited this step from being saved:
      %ul
        - step.errors.full_messages.each do |message|
          %li= message
  .actions
    .form-group
      = form.text_field :title, placeholder: true, class: 'form-control'
      = form.hidden_field :problem_id, value: problem, class: 'form-control'
    .form-group
      = form.submit